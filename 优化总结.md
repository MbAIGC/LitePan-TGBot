# LitePan-TGBot 优化总结

> 本文档汇总 LitePan-TGBot 从最初设想到当前版本的完整优化历程，包括功能迭代、设计决策、技术要点与已知局限。

## 1. 项目定位

LitePan-TGBot 是一个**零改动 LitePan 源码**的独立 Telegram Bot：它作为独立进程，把 Telegram 消息转成 LitePan 现有开放接口的 Webhook 调用，实现“在 TG 里远程触发 LitePan 自动化（刷新缓存 / STRM 生成 / 刮削 / Emby 刷库）并获取回执”。

适用模型：**每个用户私有部署自己的 LitePan + 自己的 Bot**。用户 A 部署 litepan-A + bot-A（盘：光鸭-A、123-A；任务：光鸭-A-TV、123-A-DSJ）；用户 B 部署 litepan-B + bot-B（盘：115-B、天翼-B；任务：115-B-ABC、天翼-B-123）。同一套代码，配置各自填写，互不干扰。

## 2. 优化历程

### 2.1 基础版：零源码改动的触发与回执

- 只依赖 LitePan 现有接口：`POST /api/open/automation/events`（Bearer `task` 类型 API Key）、`GET /api/health`。
- 命令体系：`/refresh [路径]`、`/run <事件> [路径]`、`/ping`、`/status`、`/start`、`/help`。
- 回执分两层：
  - 触发回执（默认）：webhook 同步返回命中的规则数与规则名，Bot 原样转发；
  - 完成回执（可选）：配置管理员账号后，Bot 用 `/api/auth/login` 建立会话，轮询 `GET /api/admin/automation/runs`，推送每条规则的 `✅/❌ + 步骤明细`。
- 纯 Python 3 标准库实现，无第三方依赖；支持 Docker Compose 部署。

### 2.2 多盘支持：澄清 LitePan 自动化模型

关键认知（源码核实）：

- **STRM 任务按“账号 + 目录”配置**，不存在跨账号的全盘扫描任务；
- **自动化规则的动作链挂的是具体任务（task_id）**，webhook 请求里的 `path` 只参与规则过滤，不传给动作；
- 后台 Webhook 触发配置只有 `event` + `source`，`message` 字段会被忽略但无害。

由此明确：`/refresh` 不带路径 ≠ 全盘刷新。全盘 = 规则动作链里挂全部盘的任务；单盘 = 每条规则挂单个盘的任务、用不同事件名区分。

### 2.3 模型澄清：每用户独立部署，零硬编码

- 按用户各自私有部署的方向修正设计，代码中不出现任何盘名、任务名、事件名；
- `users.json`（单条目为主）或 `.env` 承载各人的 LitePan 地址、API Key、盘名映射；
- `DRIVES` 支持 `别名:事件` 与 JSON 两种格式，`/refresh 光鸭-A` 按别名触发。

### 2.4 自动发现：解决“零硬编码导致操作不便”

问题：手工在配置里维护“盘名 → 事件名”等于复制 LitePan 里已有的信息，两边易失同步。

方案：Bot 配置管理员账号后自动登录 LitePan，读取：

- `GET /api/admin/accounts` → 账号名（光鸭-A、123-A）
- `GET /api/admin/automation/options` → STRM / 整理任务（含所属账号）
- `GET /api/admin/automation/rules` → 规则名、Webhook 事件名、动作挂的任务

自动生成映射，实现：

- `/refresh 光鸭-A` 直接用 LitePan 账号名匹配；精确优先、唯一子串兜底；
- `/list` 展示 `事件 → 规则名 → 涉及任务` 与可用盘名；
- `default_event=auto`（或留空）时，`/refresh` 自动采用唯一 Webhook 规则；
- **单账号规则判定**：只有“全部任务属于同一账号”的规则才计入该盘，避免 `/refresh 光鸭-A` 误触发挂多账号任务的“全盘”规则、重复跑任务；
- `DRIVES` 降级为可选手动覆盖（不想给管理员权限时使用）。

### 2.5 隐私优化

- 密码与 API Key 只在本机发往用户自己的 LitePan，**不经过 Telegram**，也不出现在回复或日志中；
- 回复默认**不显示 LitePan 地址**（`show_url` / `SHOW_LITEPAN_URL=1` 可调试开启），盘名、规则名、任务名照常显示；
- 明文配置的缓解：`chmod 600 users.json`、不进 git（已在 `.gitignore` 中排除）、可用 Docker secrets。

### 2.6 本次优化：工程化交付

- 项目更名为 **LitePan-TGBot**；
- 新增 GitHub Actions（`.github/workflows/docker-image.yml`）：push `main`、打 `v*` 标签或手动触发时，自动构建 Docker 镜像并推送 **GHCR**（`ghcr.io/<owner>/litepan-tgbot`），标签含 `latest` / `v*` / `sha-*`，使用仓库自带 `GITHUB_TOKEN`；
- 本总结文档落盘项目根目录。

## 3. 命令一览

| 命令 | 说明 |
| --- | --- |
| `/refresh` | 触发默认规则（全盘，动作链挂几个盘就刷几个） |
| `/refresh <盘名>` | 按 LitePan 账号名触发单盘规则，如 `/refresh 光鸭-A` |
| `/refresh /路径` | 触发默认规则，路径仅作记录 |
| `/list` | 自动列出账号、规则（事件→名称→任务）、可用盘名 |
| `/run <事件> [路径]` | 触发任意 Webhook 事件 |
| `/ping` | 检查本会话 LitePan 连通性 |
| `/status` | 查看配置概览（不含地址，默认）与连通性 |

## 4. 技术要点

- 触发链路：TG 消息 → 长轮询 `getUpdates` → `POST /api/open/automation/events`（Bearer Key）→ LitePan 异步动作链；
- 回执链路（可选）：`/api/auth/login` 会话 → 轮询 `/api/admin/automation/runs`，按运行 ID 增量识别新任务，终止态推送结果；
- 自动发现：3 个管理接口一次拉取，60 秒 TTL 缓存；401 时自动重新登录；
- 隐私：回复不渲染地址；密钥掩码显示；日志不打印密码；
- 兼容：`delayTime`（CloudSaver）、`message` 留痕字段均兼容。

## 5. 目录结构

```text
LitePan-TGBot/
├── tgbot.py                    # Bot 主体（纯标准库，单文件）
├── Dockerfile                  # 镜像定义
├── docker-compose.yml          # 本地部署
├── .env.example                # 环境变量模板（单用户模式）
├── users.example.json          # 用户配置模板（推荐模式）
├── README.md                   # 部署与使用说明
├── 优化总结.md                  # 本文档
└── .github/workflows/
    └── docker-image.yml        # GHCR 镜像构建 Action
```

## 6. 测试情况

离线测试覆盖（非真实网络，桩掉 LitePan/Telegram 接口）：

- 命令解析与回执文案；
- webhook 触发请求体与鉴权头（含可选 `message` 字段）；
- 401 → 自动登录 → 重试链路；
- 多用户/单用户配置路由；
- 自动发现：账号匹配（精确/子串）、单账号规则判定、全盘规则不误触；
- 隐私开关：默认隐藏地址、开启后显示。

## 7. 已知局限

- 完成回执是轮询实现，非实时推送；实时推送需在 LitePan 内置 tgbot 订阅事件总线（改源码）；
- 自动发现依赖管理员账号；未配置时退回手工 `DRIVES` 映射；
- Telegram `getUpdates` 只能单实例运行；
- 大陆网络访问 `api.telegram.org` 需代理或镜像（`TG_API_BASE`）；
- LitePan 为 PolyForm Noncommercial 1.0.0 许可证，本项目仅提供 Bot 侧能力，自用无碍。
