# 优化记录

本文件记录代码 review 发现的问题与后续优化项，按处理状态分组；每条标注建议来源模型（DeepSeek-V4-Pro / GPT-5.6 / DeepSeek-V4-Flash）。

## ✅ 已修改（2026-08-13，v0.14 / v0.15）

> 已完成修复。~~删除线~~表示已处理，并标注建议来源模型。

### 测试与 CI

- ~~1. 本地测试跑不过，且没有 requirements.txt~~ → **已修改**（DeepSeek-V4-Pro 第 1 条，Flash 复核合并为最高优先）
  - 现象：干净环境未装 pypinyin 时 `光鸭` 被转成 `pan1` 而非 `guangya`，首个断言失败。
  - 处理：新增 `requirements.txt`（`pypinyin==0.54.0`），README 补充本地测试依赖说明。

- ~~9. CI 没有跑测试~~ → **已修改**（DeepSeek-V4-Pro 第 9 条）
  - 处理：CI 新增 test job（安装依赖后运行 `test_tgbot.py` 与 `test_robustness.py`），构建依赖测试通过；仅改 .md 文档时跳过测试与构建。

- ~~工程建议：CI 增加 unittest/pytest 步骤；pypinyin 入依赖文件~~ → **已修改**（GPT-5.6 工程改进建议）
  - 处理：与上述两条合并落地（test job + `requirements.txt`）。

### 配置解析

- ~~2. 配置数字字段裸 int() 解析，非法值会让进程 traceback~~ → **已修改**（DeepSeek-V4-Pro 第 2 条）
  - 处理：统一 `ConfigError` + `_parse_int`/`_parse_bool`；`TG_ALLOWED_IDS=abc`、`lite_timeout="abc"` 不再 traceback。

- ~~3. show_url 配置存在字符串布尔值错误~~ → **已修改**（GPT-5.6 第 3 条）
  - 现象：`"show_url": "false"` 被 `bool()` 解析成 True，导致地址被显示。
  - 处理：新增 `_parse_bool()`，严格支持 true/false/1/0/yes/no/on/off，非法值直接报配置错误。

- ~~4. 用户配置缺少边界校验~~ → **已修改**（GPT-5.6 第 4 条）
  - 处理：`lite_timeout >= 1`、`receipt_poll >= 1`、`receipt_timeout >= receipt_poll`、chat_ids 必须为整数且不可重复、show_url 字段类型严格校验。

- ~~工程建议：TG_ALLOWED_IDS / chat_ids 非法值抛 ConfigError~~ → **已修改**（GPT-5.6 工程改进建议）
  - 处理：与上述配置解析合并落地。

### 消息可靠性

- ~~2. 更新游标先确认后执行，可能丢消息或重复执行~~ → **已修改**（GPT-5.6 第 2 条）
  - 处理：getUpdates 改为“处理成功才推进 offset 并原子落盘”；单条消息失败重试 5 次后跳过，避免卡死轮询；重启靠已落盘 offset 去重。

- ~~8. 状态文件非原子写入；by_event / account_slug 死代码~~ → **已修改**（DeepSeek-V4-Pro 第 8 条）
  - 处理：`_save_offset` 改临时文件 + `os.replace` 原子写；删除只写不读的 `by_event`、`account_slug`。

- ~~13. watch_run 回执死等最早运行，可能收不到后续回执~~ → **已修改**（DeepSeek-V4-Flash 复核遗漏补充）
  - 处理：按 `(rule_id, run_id)` 去重回执，同一规则多次触发各自回执，不再死等。

- ~~5. 运行状态判断过于宽松，可能提前发“完成”回执~~ → **已修改**（GPT-5.6 第 5 条）
  - 处理：显式终态 `success/failed/error/cancelled`，queued/pending/waiting 等状态继续轮询。

### 自动发现与 slug

- ~~5. 自动发现接口字段解析不健壮，/info、/refresh 静默失败~~ → **已修改**（DeepSeek-V4-Pro 第 5 条，Flash 复核建议提前）
  - 处理：`_int_field`/`_str_field` 集中容错，整体异常降级为“自动发现失败”并输出可定位日志。

- ~~4. 账号 slug 与规则 slug 未共用命名空间，规则遮蔽盘名命令~~ → **已修改**（DeepSeek-V4-Pro 第 4 条）
  - 处理：规则优先保留 slug，账号冲突自动加 `_2` 后缀，两个入口都可达。

- ~~6. 账号规则归属判断可能误分类规则~~ → **已修改**（GPT-5.6 第 6 条）
  - 处理：所有动作均成功解析且全部属于同一账号才归入单盘规则，未知任务/其他动作不再误归入。

### 文案与文档

- ~~6. /info 状态文案会误导~~ → **已修改**（DeepSeek-V4-Pro 第 6 条）
  - 处理：区分“自动发现未开启（需管理员账号）”与“自动发现失败（接口异常）”。

- ~~7. users.json 存在但为空时不回退 .env~~ → **已修改**（DeepSeek-V4-Pro 第 7 条；Flash 复核：行为合理，只改文案）
  - 处理：保持不静默回退，报错文案明确提示“请删除或改名 users.json”。

- ~~12. TG_ALLOWED_IDS 与 users.json 并存时覆盖~~ → **已修改**（DeepSeek-V4-Pro 第 12 条；Flash 复核：安全特性，文档化即可）
  - 处理：不改代码，README / .env.example 文档化“白名单以 env 为准（更严格）”的行为。

### Docker / 工程化

- ~~10. 容器以 root 运行~~ → **已修改**（DeepSeek-V4-Pro 第 10 条）
  - 处理：Dockerfile 新增非 root 用户 `app`；compose 增加 `read_only: true` + `tmpfs /tmp`。

- ~~工程建议：健康检查、只读文件系统~~ → **已修改**（GPT-5.6 工程改进建议）
  - 处理：新增 `--health` 子命令 + Dockerfile `HEALTHCHECK`；compose 只读根文件系统。

## ⏳ 待优化（尚未处理）

> 以下条目尚未处理，保持待优化状态。

- **HTTP mock 测试**：覆盖 401 自动重登录、409 冲突退避、请求超时（GPT-5.6 工程改进建议）。
  - 说明：运行状态变化已由 `test_robustness.py` 覆盖；多用户菜单隔离不适用（见下方确认不修）。

## 🚫 确认不修 / 不采纳

- **多用户菜单隔离 / 菜单命令重复**（DeepSeek-V4-Pro 第 3 条、GPT-5.6 第 1 条）：部署模型为单 LitePan 对接单 Bot、`TG_ALLOWED_IDS` 控制使用人，所有使用人共享同一份规则，全局 `setMyCommands` 菜单一致是正确行为 → **确认不修**。
- **CI action 只锁 tag 未锁 SHA**（DeepSeek-V4-Pro 第 11 条）：用户确认不需要 → **不采纳**。
- **Docker 基础镜像固定 digest**（GPT-5.6 工程改进建议）：曾实施，后按用户要求取消 → **不采纳**。

## 复核结论（DeepSeek-V4-Flash，2026-08-13）

- 清单内条目均经实际验证，内容真实；
- 第 7、12 条：行为合理，建议只改文案/文档（已按此执行）；
- 第 3 条（多用户菜单）：实际触发概率低、修复成本较高，建议排后（后确认不修）；
- 第 1 + 9 条：合并为最高优先，先建安全网（已执行）；
- 第 5 条（自动发现）：依赖第三方 API 的核心路径，应提前修复（已执行）；
- 补充第 13 条 watch_run 回执风险（已修复）；
- 修复顺序：测试与 CI → 配置/接口解析 → slug 冲突/状态文件原子性 → 多用户菜单 → 文档打磨（已按此顺序在 v0.14 执行，多用户菜单除外）。
