# 优化记录

本文件记录代码 review 发现的问题与后续优化项，按处理状态分组。

## 待优化（2026-08-13 代码 review）

> 状态：待优化。以下问题来自对 `tgbot.py`、`test_tgbot.py`、`Dockerfile`、CI 配置及使用文档的完整 review，尚未处理。

### 会导致崩溃或功能失效的问题

1. 本地测试跑不过，且没有 `requirements.txt`
   - 现象：在未安装 `pypinyin` 的干净环境里执行 README 中的 `python3 test_tgbot.py`，首个断言即失败，`光鸭` 被转成 `pan1` 而不是预期的 `guangya`。
   - 原因：`pypinyin` 是可选依赖，但仓库没有 `requirements.txt`，README 的本地测试指引也没有说明需要安装它。
   - 建议：新增 `requirements.txt`，并在 README/CI 中补充 `pip install pypinyin==0.54.0`，或让测试在缺少依赖时明确跳过。

2. 配置中的数字字段用裸 `int()` 解析，非法值会让进程直接 traceback 崩溃
   - 位置：`tgbot.py:297-299`、`tgbot.py:339`。
   - 现象：`TG_ALLOWED_IDS=abc`、`users.json` 中 `"lite_timeout": "abc"` 会抛出未捕获的 `ValueError`，而不是友好的 `ConfigError`。
   - 建议：统一封装为 `ConfigError`，并对 `lite_timeout`、`receipt_poll`、`receipt_timeout` 增加大于 0 的范围校验（`receipt_poll=0` 会造成忙循环）。

3. 多用户模式下菜单命令重复，导致 `setMyCommands` 失败
   - 状态：确认不修（2026-08-13）。部署模型为一个 LitePan 实例对接一个 LitePan-TGBot，使用人由 `TG_ALLOWED_IDS` 白名单控制，所有使用人共享同一实例的同一份规则，全局 `setMyCommands` 保持菜单一致是正确行为；多用户菜单隔离不适用。
   - 位置：`tgbot.py:885` 附近的 `_refresh_menu_locked`。
   - 现象：slug 去重只在单个 profile 内部进行，不同用户各有一条同名规则时，会生成两条相同的 `refresh_<slug>` 命令，Telegram 会拒绝并导致菜单更新失败。
   - 建议：跨 profile 全局去重；同时 `setMyCommands` 未传 `scope`，菜单为全局共享，多用户时会互相看到对方规则名，需明确是否接受或按 scope 生成。

4. 账号 slug 与规则 slug 未共用命名空间，规则命令会遮蔽盘名命令
   - 位置：`tgbot.py:675`（`refresh_slug` 先查规则再查账号）、`tgbot.py:482`、`tgbot.py:496`。
   - 现象：账号名与规则名相同时，二者会生成相同 slug，`/refresh_<slug>` 永远命中规则，账号级“触发该盘全部单盘规则”的入口无法访问。
   - 建议：账号与规则统一维护 `used` 集合，或在冲突时采用可区分的命名/优先级策略。

5. 自动发现接口的字段解析不健壮，一个脏字段就会让 `/info`、`/refresh` 静默失败
   - 位置：`tgbot.py:434` 等处的 `int(... or 0)` 转换。
   - 现象：远端 API 字段缺失或类型异常时会抛 `ValueError`/`TypeError`，而 `_discovery` 只捕获 `LitePanError`，异常会冒泡到消息处理的兜底逻辑，用户收不到任何回复。
   - 建议：字段解析集中做容错处理，异常统一降级为“自动发现失败”，并输出可定位的日志。

### 次要 / 健壮性问题

6. `/info` 状态文案会误导：`tgbot.py:823` 将自动发现结果为 `None` 统一显示为“自动发现未开启（需管理员账号）”，实际可能只是请求失败。

7. `users.json` 存在但为空时不会回退到 `.env` 单用户模式：`tgbot.py:362-377` 的回退逻辑挂在 `elif` 上，只覆盖文件不存在的情况。

8. 状态文件非原子写入：`_save_offset` 直接覆盖写 JSON，进程中途被杀可能损坏游标文件，重启后 `offset` 回退导致 Telegram 重推旧消息。另有 `by_event`、`account_slug` 两个字段只写不读，属于死代码。

### 安全 / 工程化问题

9. CI 没有跑测试：`.github/workflows/docker-image.yml` 只做镜像构建和推送，`test_tgbot.py` 完全未执行，回归问题无法被拦截。

10. 容器以 root 运行：`Dockerfile` 未创建并切换到非 root 用户，建议增加 `USER` 收紧权限。

11. CI 的 action 只锁 tag 未锁 SHA：`actions/checkout@v4`、`docker/build-push-action@v6` 等写法存在供应链风险，建议锁定到具体 commit。

12. `TG_ALLOWED_IDS` 与 `users.json` 同时存在时会覆盖而不是合并：`tgbot.py:347` 中 env 有值就完全忽略 `users.json` 的 chat_ids，残留配置时容易造成“明明写进 users.json 却提示未授权”的反直觉现象。

## 复核意见（2026-08-13，待确认）

> 状态：待确认。以下是对上方待优化清单的内容评估，供后续决定优先级参考，尚未形成最终执行计划。

总体评价：清单内每条均经实际验证，内容真实；但严重度排序可以调整——部分条目被高估，部分被低估，另有一条 review 时发现但未写入清单的遗漏。

### 被高估的条目

- 第 7 条（`users.json` 存在但为空时不回退 `.env` 单用户模式）：不完全是 bug。文件存在即表示用户选择了 users.json 模式，内容未填对时报错，比静默回退到 `.env` 更安全。建议只优化报错文案，不动行为。
- 第 12 条（`TG_ALLOWED_IDS` 覆盖 users.json）：属于安全特性在起作用，白名单更严格反而更安全。主要问题是文档未说明该行为，建议文档化而非改代码。
- 第 3 条（多用户菜单命令重复）：问题真实存在，但项目定位是“每用户私有部署自己的 Bot”，多用户只是附带能力，实际触发概率低，修复成本较高，建议排后。（2026-08-13 已确认不修：单 LitePan 对接单 Bot、`TG_ALLOWED_IDS` 控制使用人，菜单全局一致是正确行为。）

### 被低估的条目

- 第 1 条 + 第 9 条（测试跑不过 / CI 不跑测试）：是同一问题的一体两面——仓库里有测试但没人执行，坏测试才能被推到 main。建议合并为最高优先级一并修复（补依赖声明 + CI 增加测试步骤）。
- 第 5 条（自动发现字段解析脆弱）：这是依赖第三方 API 的核心路径，接口结构稍有变化，`/info`、`/refresh` 就会静默失灵且异常被吞掉。防御性解析应往前排，不应归为次要问题。

### 遗漏补充（建议新增为第 13 条，待确认）

`watch_run` 回执逻辑：当前取“比快照更大的最小运行 ID”。同一规则短时间内被触发两次时，会死等第一个运行结束；若第一个运行卡住，第二个早已完成也收不到回执，只能等到超时。这属于“完成回执”核心功能上的真实风险，建议补充为待优化条目。

### 建议修复顺序（待确认）

1. 第 1 + 9 条：测试与 CI（一次解决，先建立安全网）
2. 第 2、5 条：配置与接口解析的健壮性
3. 第 4、8 条：slug 冲突、状态文件原子性
4. 第 3 条：多用户菜单
5. 第 6、10、11、12 条及第 7 条文案：文档与打磨项（第 7 条只改文案，不动逻辑）

## GPT-5.6 review（2026-08-13，待优化）

> 状态：待优化。以下为 GPT-5.6（terra）对 `tgbot.py` 的独立 review 意见，尚未处理。

### 主要问题

1. 多用户模式下命令菜单会互相覆盖
   - 状态：确认不修（2026-08-13）。部署模型为一个 LitePan 实例对接一个 LitePan-TGBot，使用人由 `TG_ALLOWED_IDS` 白名单控制，所有使用人共享同一实例的同一份规则，菜单应当全局一致；多用户菜单隔离不适用。
   - 位置：`tgbot.py:654`（/info）、`tgbot.py:657`（/menu）调用 `refresh_menu(profile)`，`tgbot.py:862` 使用全局 `setMyCommands`。
   - 现象：某个用户刷新菜单后，菜单内容会变成该用户的规则，其他用户的规则可能消失；不同 LitePan 实例中相同 slug 也会产生描述冲突。
   - 建议：使用 Telegram 的 scope 按 chat_id 设置菜单，或只注册静态命令，不把多用户规则合并到全局菜单。

2. 更新游标处理可能导致消息丢失或重复执行
   - 位置：`tgbot.py:606` 在处理消息前就推进 offset，处理异常后仍保存游标。
   - 现象：进程崩溃或处理异常时，消息可能被确认但没有成功执行；调用 LitePan 成功后进程崩溃，重启又可能重复触发任务。
   - 建议：明确采用 at-least-once 投递并为命令增加幂等键；将更新处理结果和游标持久化设计清楚，避免“先确认、后执行”。

3. show_url 配置存在字符串布尔值错误
   - 位置：`tgbot.py:300` 使用 `bool(raw.get("show_url", False))`。
   - 现象：JSON 中写 `"show_url": "false"` 会被解析成 True，导致地址被显示。
   - 建议：增加统一的 `parse_bool()`，严格支持 true/false、1/0 等格式，非法值直接报配置错误。

4. 用户配置缺少边界校验
   - 位置：`tgbot.py:297` 直接转换超时参数且无范围检查；`tgbot.py:373` 的 chat_ids 重复时静默覆盖前一个用户配置。
   - 建议校验：`lite_timeout >= 1`；`receipt_poll >= 1`（0 会造成忙轮询，负数使 `time.sleep()` 抛异常）；`receipt_timeout >= receipt_poll`；chat_ids 必须为整数且不可重复；show_url、drives 等字段类型必须正确。

5. 运行状态判断过于宽松，可能过早发送“完成”回执
   - 位置：`tgbot.py:966` 将所有不等于 "running" 的状态都视为终态。
   - 现象：LitePan 使用 queued、pending、waiting 等状态时，机器人会提前发送完成结果。
   - 建议：显式定义终态，例如 success、failed、error、cancelled，其他状态继续轮询。

6. 账号规则归属判断可能误分类规则
   - 位置：`tgbot.py:452` 只要发现一个已知任务属于某账号，就可能把规则归入该账号；规则中的未知任务或其他动作不会阻止归类。
   - 现象：可能导致 `/refresh <盘名>` 触发包含额外任务的规则。
   - 建议：记录所有动作的解析结果，只有“所有相关任务均成功解析，且全部属于同一账号”时，才归入单盘规则。

### 工程改进建议

- 在 GitHub Actions 中加入 `python3 -m unittest` 或 pytest 测试步骤；
- 将 pypinyin 放入明确的依赖文件，或让测试在未安装时跳过拼音相关断言；
- Docker 镜像基础镜像固定 digest，并考虑非 root 用户、健康检查和只读文件系统；
- 对 `TG_ALLOWED_IDS`、chat_ids 的非法值抛出 ConfigError，目前可能直接产生 traceback；
- 增加真实 HTTP mock 测试，覆盖 401 重登录、409 冲突、超时、运行状态变化等场景（多用户菜单隔离不适用，见主要问题第 1 条）。

### 总体评价

代码结构清晰，权限白名单、API Key 不回传、规则 ID 精确执行等方向正确；当前最需要优先修复的是多用户菜单隔离和消息游标/幂等问题。
